create table PROPERTIES (
  APPLICATION varchar(100),
  PROFILE varchar(100),
  LABEL varchar(100),
  KEY varchar(100),
  VALUE varchar(100)
);

create table CUSTOM (
  APP varchar(100),
  NAME varchar(100),
  VAL varchar(100)
);